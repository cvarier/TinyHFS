#ifndef TINY_HFS_H
#define TINY_HFS_H

/*short nextAvailableFileAddress = FOLDER_UPPER_BOUND + 1;
short latestFreedFileHeaderSpaceAddress = FREE_FILE_HEADER_SPACE_MEM_LOC_LOW;
short latestFreedFileSpaceAddress = FREE_FILE_SPACE_MEM_LOC_LOW;
short latestFreedFolderSpaceAddress = FREE_FOLDER_SPACE_MEM_LOC_LOW;

//Messing with Linked Lists

/*typedef struct fileNode {
  
   char name[MAX_NAME_LENGTH];
   short dateModified;
   short dateCreated; 
   
};


typedef struct folderNode {
    char name[MAX_NAME_LENGTH];
    short dateModified;
    short dateCreated;
    struct folderNode * next;
    struct fileNode * fileNode;
    
} root;
*/

#endif
